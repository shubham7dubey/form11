




<p id="notice"><%= notice %></p>


<div style="color:black;">

  <% if current_user.role =='admin' or current_user.role=='admin' %>
  <div>
  <%= form_tag notices_reg_id_path do %>
  <p>Enter ID</p>
  <%= text_field_tag(:regid) %>
  <br>
  <%= submit_tag "search" %>

  <% end %>

  <%= @id %>
 </div>





  
  <% end %>


<center><h1>Notices</h1>
<div class="logout" style="margin-top: 90px;margin-right: 30px; ">
  <% if current_user.role=='Hod' or current_user.role=='hod' %>
    <button class="button"><a href="/notices/new">Create Notice </a></button>
  <% end %>
</div>
<div class="table-responsive">
<table class="table" border="2px">
  <thead>
    <tr>
      <th>Notice ID</th>
      <th class="danger">Circular title</th>
      <th>Select file</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @notices.each do |notice| %>
      
      <tr>
        <% if current_user.role =='hod' or current_user.role =='Hod' %>
        <td><%= notice.id %></td>
        <td><%= notice.Circular_Title %></td>
        <% else %>
        <td> <%= notice.Circular_Title %></td>
        <% end %>
        <td><%= notice.Select_File %></td>
        <td><%= link_to 'Show', notice %></td>
        <% if  current_user.role =='hod' or current_user.role =='Hod' %>
        <td><%= link_to 'Edit', edit_notice_path(notice) %></td>
        <td><%= link_to 'Destroy', notice, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table></center> 
</div>
<p style="color: black;" >
</p>
<br>



<style >
  
.button {

  float: right;
  background-color: #a6b3f3; 
  border: none;
  color: white;
  padding: 20px;
  text-align: center;
  text-decoration: none;
  display: inline-block;
  font-size: 16px;
  margin: 4px 2px;
  cursor: pointer;
  border-radius: 12px;
  margin-right: 15px;
} 



</style>


<br><br>

<% if current_user.role =='hod' or current_user.role=='Hod' %>

 <%= form_tag notices_send_email_path do %>
 <%= check_box_tag(:faculty) %>
 <%= label_tag(:faculty,"Faculty")%>
 <%= check_box_tag(:student) %>
 <%= label_tag(:student,"Student")%>
 <%= check_box_tag(:staff) %>
 <%= label_tag(:staff,"Staff")%>
 <br>
  <p>Email</p>
 <%= text_field_tag(:mail) %>

<p>Enter notice id</p>
 <%= text_field_tag(:id) %>
<br>

 <%= submit_tag "send email" %> <!-- this is the submit tag -->
 <%end%>
  
<% end %>


















</div>




